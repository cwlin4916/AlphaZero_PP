# Archive

This directory contains superseded code from earlier phases of the project.

## nsai_experiments/

The original AlphaZero implementation and game experiments (zoning game, line-extending game, etc.) that preceded the current `alphazeropp` package. Kept for reference; no active code depends on it. A few legacy tests in `tests/` still exercise this code.
